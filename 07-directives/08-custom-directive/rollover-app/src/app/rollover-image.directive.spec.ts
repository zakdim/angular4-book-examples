import { RolloverImageDirective } from './rollover-image.directive';

describe('RolloverImageDirective', () => {
  it('should create an instance', () => {
    const directive = new RolloverImageDirective();
    expect(directive).toBeTruthy();
  });
});
