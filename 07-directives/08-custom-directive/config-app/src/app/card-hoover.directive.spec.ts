import { CardHooverDirective } from './card-hoover.directive';

describe('CardHooverDirective', () => {
  it('should create an instance', () => {
    const directive = new CardHooverDirective();
    expect(directive).toBeTruthy();
  });
});
