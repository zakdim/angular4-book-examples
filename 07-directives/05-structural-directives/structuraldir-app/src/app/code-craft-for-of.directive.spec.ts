import { CodeCraftForOfDirective } from './code-craft-for-of.directive';

describe('CodeCraftForOfDirective', () => {
  it('should create an instance', () => {
    const directive = new CodeCraftForOfDirective();
    expect(directive).toBeTruthy();
  });
});
