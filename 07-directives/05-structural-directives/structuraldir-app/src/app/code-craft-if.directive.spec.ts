import { CodeCraftIfDirective } from './code-craft-if.directive';

describe('CodeCraftIfDirective', () => {
  it('should create an instance', () => {
    const directive = new CodeCraftIfDirective();
    expect(directive).toBeTruthy();
  });
});
